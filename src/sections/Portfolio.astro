---
import Duo from "../components/Duo.astro";
import CurrentlyBuildingContainer from "../components/CurrentlyBuildingContainer.astro";
import SectionHeading from "../components/SectionHeading.astro";
const posts = await Astro.glob("../portfolio-article/*.md");

// const colors = ["teal-200", "emerald-400", "yellow-200"];
---

<SectionHeading>Recent Projects</SectionHeading>

{
  posts
    .sort((a, b) => a.frontmatter.order - b.frontmatter.order)
    .map((post, index) => {
      if (!!post.frontmatter.currentlyBuilding) {
        return (
          <CurrentlyBuildingContainer>
            <post.Content />
          </CurrentlyBuildingContainer>
        );
      } else {
        return (
          <Duo>
            <post.Content />
          </Duo>
        );
      }
    })
}
