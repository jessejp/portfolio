---
const { frontmatter } = Astro.props;
---

<div class="flex w-11/12 items-center justify-between flex-col md:flex-row">
  <div class="w-auto md:w-1/3 max-md:self-start">
    <h3 class="text-2xl font-semibold">{frontmatter.title}</h3>
    <slot />
    <div class="my-6 py-2 px-5 border-slate-900 border-2 w-fit">
      <a
        class="text-xl "
        href={frontmatter.href}
        target="_blank">Link to project</a>
    </div>
  </div>
  <img
    class="w-full max-w-sm max-md:mt-6 max-md:self-start"
    src={frontmatter.image}
    alt={frontmatter.title}
  />
</article>
<style>
  p {
    margin-top: 1rem;
  }
</style>
